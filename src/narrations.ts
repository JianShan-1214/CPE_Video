/**
 * 每個步驟的字幕文字，索引對應 code01.cpp, code02.cpp, ...
 * 之後可替換成實際語音檔的腳本
 */
export const narrations: string[] = [
  // code01 - 說明
  "今天我們來學習氣泡排序法（Bubble Sort）。它的原理是：反覆比較相鄰的兩個元素，如果順序不對就交換，讓較大的值慢慢往後移，就像氣泡浮出水面一樣。",

  // code02 - includes
  "首先引入 iostream 頭文件，讓我們可以使用 cout 進行輸出。接著 using namespace std，這樣之後就不需要每次都加 std:: 前綴了。",

  // code03 - bubbleSort 函式骨架
  "宣告排序函式 bubbleSort，接收一個整數陣列 arr 以及元素個數 n 作為參數，它負責執行整個排序的邏輯。",

  // code04 - 外層迴圈
  "加入外層迴圈，i 從 0 跑到 n 減 2，總共執行 n 減 1 輪。每完成一輪，當前最大的元素就會移動到正確的位置。",

  // code05 - 內層迴圈
  "加入內層迴圈，j 從 0 跑到 n 減 i 減 2。為什麼要減 i？因為每輪結束後，末端已排好的元素不需要再比較了，範圍逐漸縮小。",

  // code06 - if 判斷
  "這裡是核心判斷：如果 arr[j] 大於 arr[j+1]，也就是前面的元素比後面大，就需要交換這兩個元素的位置。",

  // code07 - 交換邏輯
  "執行交換時，需要一個暫時變數 temp。先把 arr[j] 暫存到 temp，再把 arr[j+1] 移到 arr[j]，最後把 temp 放回 arr[j+1]，完成交換。",

  // code08 - printArray
  "新增 printArray 輔助函式，用迴圈逐一輸出陣列的每個元素，加上空格分隔，最後換行。這樣我們就能清楚看到排序前後的結果。",

  // code09 - main + 陣列
  "在 main 函式裡建立測試用的整數陣列，包含 7 個未排序的數字：64、34、25、12、22、11、90，這就是我們要排序的資料。",

  // code10 - 排序前輸出
  "呼叫 printArray 印出排序前的陣列，讓觀看者看清楚原本的順序是什麼樣子，等一下排序後就能直接比較差異。",

  // code11 - 呼叫排序
  "呼叫 bubbleSort 函式，傳入陣列 arr 和元素個數 n，開始執行排序。這一行是整個程式的核心呼叫。",

  // code12 - 排序後輸出 + return
  "排序完成後，再次呼叫 printArray 印出結果，你會看到數字已經從小到大整齊排列。這就是完整的氣泡排序法！",
];
